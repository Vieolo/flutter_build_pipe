name: build_pipe
description: Build pipline for Flutter apps for different target platforms
version: 0.2.0
repository: https://github.com/vieolo/flutter_build_pipe

environment:
  sdk: ^3.8.1

# Add regular dependencies here.
dependencies:
  yaml: ^3.1.3
  path: ^1.9.1
  crypto: ^3.0.6

dev_dependencies:
  lints: ^6.0.0
  test: ^1.24.0

executables:
  build_pipe: build
